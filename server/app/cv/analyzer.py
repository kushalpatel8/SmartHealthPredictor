from app.cv.preprocessing import resize_image, normalize_image

def analyze_scan(image_path: str):
    # Placeholder for running a CV model over an X-ray or scan
    return {
        "status": "success",
        "findings": "No anomalies detected (placeholder)"
    }
